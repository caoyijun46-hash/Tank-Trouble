# 联机开发交接（2026-09 · 阶段 1 完成）

> 写给未来的 Claude / 开发者。项目根目录此文档是联机工作线的权威上下文。
> 时间线：阶段 1（主机权威 + 状态同步 + 客户端插值）的同步管道已打通并验证。

## 一、已完成：联机阶段 1（UDP 快照 + 插值）

**架构**（FPS 家族路线，非格斗锁步）：主机本地完整模拟 → 20Hz 广播快照 → 客户端纯"快照播放器"（插值缓冲渲染过去 ~150ms）。

### 当前 Net 域文件（Assets/Scripts/Net/）

| 文件 | 职责 |
|---|---|
| `NetManager.cs` | MonoBehaviour 单例（Host/Client 双角色）：driver 生命周期、Accept while+NativeList 存连接、tick 广播、Client 收快照推送、每秒心跳日志 |
| `SnapshotProtocol.cs` | 21B 快照格式：type(1)+seq(4)+hostTime(4)+x/z/yaw(12)。`SnapshotData` struct。**注意：hostTime 用 Time.unscaledTime（与广播节拍同基准，慢放/暂停不失真）** |
| `InterpBuffer.cs` | 客户端插值核心：窗口(s0,s1)+进度按窗口时间跨度推进（unscaledDeltaTime，免绝对时钟同步）；耗尽冻结在端点；yaw 必须 LerpAngle |
| `NetSyncObject.cs` | 假对象载体：Host 端沿圆轨迹自走；Client 端从缓冲取姿态渲染。将来被真实坦克替换 |

### 接线（两个 Editor = ParrelSync 主项目/clone）
- 每端场景一个空物体挂 NetManager：role = Host / Client，port = 7778，syncObject 拖一个挂 NetSyncObject 的 Cube
- Client 的 serverIp = 127.0.0.1（同机回环）

### 验证状态
- Host 心跳每秒 1 次 `每秒广播 ~20 包`；Client `每秒收到 ~20 包`；Client Cube 平滑（目前轻微卡顿，见"待办"）
- MinimalPing/MinimalBroadcast 诊断脚本已删除（它们是定位根因的关键工具，git 历史里有）

## 二、⚠️ 本次调试的根因与踩坑清单（未来必读，防止重蹈覆辙）

**根因**：`DataStreamWriter` 是 struct。`SnapshotProtocol.WriteSnapshot(DataStreamWriter w, ...)` 按值传参 → 方法内写入推进的是拷贝的位置游标 → 调用方 writer 位置仍为 0 → `EndSend` 发出 **0 字节空包** → 对端 Data 事件 reader 无效（Length/ReadByte 抛 NRE）。**修复：writer 必须 `ref` 传递**（读者 `DataStreamReader` 按值传没事，读后即弃）。

踩坑清单（都是真事）：
1. `driver.PopEvent(out connection, ...)` 无事件时会把 out 参数写成 default → 覆盖已存句柄。用 `PopEventForConnection` 或 `connection.PopEvent(driver, out stream)`。
2. UTP 服务端新连接要 `driver.Accept()`（while 循环）取出，不是 PopEvent 的 Connect 事件。
3. 心跳/日志计时器必须放在每帧调用路径累计——放 tick 段内会导致 18 秒才打印一次（当时 355 包/s 的假象差点带偏整个调试）。
4. 调试"完整失败版"效率极低；**从已知可工作的最小版本出发、一次只加一个差异**（用户称之为"忒修斯之船"）——第一块板就暴露根因。
5. transport 是 Unity 6.5 内置包（6.5.0），版本线独立于 2.x，changelog 查 docs.unity3d.com/Packages/com.unity.transport@6.5/。

## 三、已知遗留与待办

1. **Client Cube 轻微卡顿**（"卡卡地旋转"）：管道已通，平滑度问题。排查方向：InterpBuffer 缓冲深度（现 3，可试 4-5）、快照率（20Hz→30Hz）、窗口滑动的冻结边角逻辑。阶段 2 前应解决（这决定手感基线）。
2. **Host 连接列表会累积"幽灵连接"**（客户端进程退出无 UDP 断开通知）：当前无害（只多几次无效发送），但心跳的"已接入 N 客户端"会虚高。将来可加超时清理或消费 Disconnect 事件。
3. **NetManager 心跳/日志可精简**：稳定后可降频或去掉。
4. **玩法接入（阶段 2 的核心工作）**：现在同步的是假对象 Cube。接真坦克时需要：
   - 远端坦克去掉本地输入/逻辑，位置由快照驱动（挂 NetSyncObject 类似物或改 NetSyncObject 支持任意 Transform）
   - 玩家自己的坦克将来走预测（阶段 3），不吃插值
   - GameManager 的胜负/道具/子弹分阶段接入

## 四、下一步规划（联机主线）

- **阶段 2：输入上行**（客户端操作命令 → 主机执行）。命令走可靠通道（UTP pipeline 或自研重传）；需要 UTP 的可靠管道 API（`driver.CreatePipeline(typeof(ReliableSequencedPipelineStage))`）——未验证，接入时先在最小脚本验证。
- **阶段 3：客户端预测**（自己的坦克本地先动 + 主机快照纠正 reconcile）。
- **阶段 4：事件通道**（击杀/得分/道具走独立消息，不进快照）。
- **公平性**：1v1 下 host advantage 结构性存在，阶段 3 只解决一半；若做真 1v1 公平需评估。
- **网络环境**：校园网 AP 隔离 → 测试用手机热点；长期方案 Unity Relay（传输层换接口，同步代码不变）。
- **清理**：阶段 1 假对象替换后删 NetSyncObject 或改造。

## 五、其它功能线状态（本会话前段已完成，均 commit）

- 计分 HUD + 暂停面板（UI Toolkit 三面板：MainAI/MainDouble/PauseButton/PauseMenu，PauseController 收口 reload 绑定）
- 音频（AudioManager prefab 全局单例：BGM/SFX 双音量 PlayerPrefs；UI 点击音、fire/crash）
- 粒子（移动 dirt、死亡爆炸，TankBase.Die 分流清场）
- 结算慢放（timeScale 0.12）+ 相机特写（FixedMapCamera Focus/WatchFullMap）
- 数值全部 Config 化（Unit/Ai/Round/Maze/Camera/Ballistic SO，Assets/Config/）
- AI 拾取道具（空手优先 GetItem，chaseTarget 共用跟随执行层）
- **遗留**：TankAI.ExecuteShoot 的 `//Fire()` 仍注释（用户测试期觉得 AI 太强；恢复即解注）；Laser 预览线由 SetPower 驱动已接好

## 六、测试与纪律（摘要，详见 CLAUDE.md）

- ParrelSync 双 Editor：主项目 = 一端，clone = 另一端，127.0.0.1:7778
- 改 Net 代码先在两端的 Minimal 验证脚本确认（如果重新引入诊断脚本，记得删）
- 快照/命令可靠性按数据类型选；客户端不拥有世界状态；插值 yaw 用 LerpAngle
