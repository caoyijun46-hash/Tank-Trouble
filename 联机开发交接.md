# 联机开发交接（2026-09-12 · 全链路完成并真机验证）

> 写给未来的 Claude / 开发者。本文件是联机工作线的权威上下文；改 Net 代码前先读。
> 项目约定的权威在 CLAUDE.md，本文件只覆盖联机线。
> 时间线：快照+插值 → 实体注册表 → 输入上行 1v1 → 对局事件/演出/局域网发现 → 配置拆分 → 首次 Windows 打包 → 真机验证（防火墙/设备差异是主要排障方向，见第五、六节）。

## 〇、设计定案（勿走回头路）

**全量对象同步：client 端没有"自己的车"，一切实体一视同仁。**

- client 是纯快照播放器：场景所有实体（坦克/道具/子弹/激光/导弹）由 host 进注册表采集，每网络帧全量广播，client 统一插值渲染。**client 的玩家车与其它坦克没有任何区别**。
- **预测已永久放弃**（含"本地操作车直通/浅窗/本地模拟"一切形态）。原因：本地状态与主机状态永不一致，产生的抖动原理性不可去除。曾有两版多对象方案按此方向实现过（本地账本+纠偏），均被整体推翻——不要再尝试。
- 推论：client 端壳上**不能有 Rigidbody/玩法脚本**（会顶掉插值写入的 transform）；一切裁决（比分/暂停/机位/死因）由 host 做，client 只呈现；联机手感旋钮只有两个——快照率与插值深度（都在 NetConfig）。

## 一、当前架构与关键常量

- 游戏端口**固定 7783**（场景序列化值）；发现公告 7781（广播）；join 7782（单播）。历史：曾实现"端口池 + 公告携带实际端口"（发现协议 v2），真机排障期间整体回退——固定端口对局域网够用，**勿再折腾端口池**。
- 快照 33Hz（NetConfig.snapshotInterval=0.03）；插值深度 3 帧；输入上行 20Hz；壳超时 3s；房间过期 3s。
- 联机门：`GameConfig.Mode != Online` → NetManager 整组件休眠（单机零开销）。直接 Play Main = Online host（调试入口）；Menu 按钮写入 Mode 后进场景。
- 可靠性按数据类型选：**状态流走不可靠**（实体帧自描述，丢帧下帧全量纠正）；**一次性事实走可靠**（Spawn/Despawn/迷宫参数/对局事件/命令 Fire）。

### 消息表（同一 UDP 流，首字节 type 分派；手写字节，无 JSON）

| type | 消息 | 管道 | 尺寸与要点 |
|---|---|---|---|
| 0 | 实体帧 | 不可靠 | 10B 头(seq+hostTime+count) + N×14B(id+x+z+yaw+power)；hostTime=unscaledTime |
| 1 | 命令 Move | 不可靠 | 14B，带自增 seq——主机只应用更新的（迟到的旧输入弃，否则坦克倒退） |
| 1 | 命令 Fire | 可靠 | 6B，一次性按下 |
| 2 | 迷宫参数 | 可靠 | 21B(cols/rows/seed/loopMin/loopMax)，client 同参生成同图 |
| 3 | Spawn | 可靠 | 15B(id+typeKey+x/z/yaw)，壳的创建 |
| 4 | Despawn | 可靠 | 3B(id+reason)，reason 0=清场 1=击杀（决定 client 放不放爆炸） |
| 5 | RoundEnd | 可靠 | 22B(winner+双方比分+camFlag/camX/camZ)，机位=host 裁决的特写 |
| 6 | RoundStart | 可靠 | 9B(双方比分)，client 相机回全图 |
| 7 | Pause | 可靠 | 2B，host 专属操作，client 只读遮罩 |
| 8 | SessionEnd | 可靠 | 2B，host 点 Menu → client 回菜单 |

### 实体壳表（typeKey，改表时 NetSyncEntity/NetManager Tooltip 与 EntityProtocol 注释同改）

| typeKey | host 端 prefab | client 端壳 |
|---|---|---|
| 0 | Tank / Enemy | Dead Tank |
| 1 | Tank 1 | Dead Tank 1 |
| 2 | Item | Dead Item |
| 3 | Bullet | Dead Bullet |
| 4 | Laser | Dead Laser |
| 5 | Missile | Dead Missile |

### Net 域文件（Assets/Scripts/Net/；Protocol / Discovery / Client 为内部子目录）

| 文件 | 职责 |
|---|---|
| `NetManager.cs` | 枢纽：driver 生命周期、注册表（id 占用表）、33Hz 广播、accept 补发、全部下行事件出口（ScoreSynced/PauseSynced/SessionEnded）、SendCommand |
| `NetConfig.cs` | 联机参数 SO（快照率/插值深度/壳超时/输入频率/房间过期）——手感单一来源 |
| `NetSyncEntity.cs` | 注册组件（挂 host 玩法 prefab）：OnEnable 注册/OnDisable 注销；typeKey；Killed 标记 |
| `ISyncHost.cs` | 命令面契约：ApplyCommand（仅 Tank 实现；姿态采集已归 NetSyncEntity，无第二本账） |
| `Protocol/`（5 个） | 纯编解码：`Snapshot` / `Command` / `MapParams` / `Entity` / `Round`（各 *Protocol.cs 文件头有字节布局注释） |
| `Discovery/`（3 个） | 发现层：`DiscoveryProtocol`（7781/7782，独立 UdpClient+BitConverter，不经 UTP）、`LanBeacon`（host 公告广播+收 join）、`LanDiscovery`（client 收公告+房间表） |
| `Client/`（5 个） | 仅 client 侧组件：`InterpBuffer`（插值缓冲；冻结必须饱和 progress=1）、`SnapshotPlayer`（壳渲染：绑定 id、出生 Prime、插值驱动、power 驱动瞄准线）、`ClientInputSimulator`（键盘输入源 20Hz 上行）、`MotionDirt`（壳扬尘）、`SpawnSound`（壳出生开火音，统一声源防双响） |

## 二、各块关键机制（实现要点速查）

- **实体注册表**：id 用 256 位占用表取"首个空闲"（不用自增计数器——换局轮回会撞仍存活的旧 id）；池化子弹靠 OnEnable/OnDisable 覆盖"出生/死亡/清场/休眠"全部增删路径。**accept 补发**=新 client 接入时重发迷宫参数+存量 Spawn（中途加入路径）；壳超时 3s 兜底清理断线残壳（正常 Despawn 可靠几乎不丢）。
- **命令上行**：OnCommand → 化身 Tank.ApplyCommand（IsRemote 模式：Move 写输入缓存、Fire 置积压标志，物理照常跑）。1v1 单化身（GameManager 联机分支 RegisterRemoteAvatar）。
- **开始一局的联机分支**：GameManager 按 Mode=Online 生成 Tank（宿主用）+ Tank 1（IsRemote 化身），并把化身注册给 NetManager。
- **暂停/返回**：CanPause 仅 host（client 端只读遮罩）；client 点返回 → host 收命令 → HostSendSessionEnd + 0.3s 真实时间延迟后 LoadScene（留发送窗口）；Restart 广播 Pause(false)（否则 client 遮罩卡死）。
- **演出联动**：TankBase.Die → NetSyncEntity.NotifyKilled → Despawn(reason=1) → client 爆炸；清场 reason=0 静默。RoundEnd 带 host 的 FixedMapCamera 机位，client 复刻特写。
- **局域网发现**：host 每秒广播公告（名字+state 等待中/游戏中）；client join 单播每秒重发（点"等待中"房间进入等待态，30s 超时回列表）。**等待态不预连接**——host 菜单阶段没有游戏监听，连接等 host 进 Main 后再建（规避跨场景连接延续）。
- **配置**：NetConfig 为唯一手感源；尘阈值 UnitConfig.dirtSpeedThreshold 两端同源；Missile 追踪参数在 BallisticConfig。

## 三、踩坑清单（累计，勿再犯）

1. **writer 必须 ref**：DataStreamWriter 是 struct，按值传发出 0 字节空包（阶段 1 根因）。
2. **reader 跨函数必须 ref**：头+实体分段读时按值传会让后续段永远从 0 重读（表现为 id 恒 0/NaN）。"reader 按值没事"只对单函数内一次读完成立。
3. **InterpBuffer 冻结必须饱和 progress=1**：断流期不封顶会累积（实测 47），恢复后每次 Push 只减 1 永远追不平 → 永久直通阶跃（历史"Client Cube 卡顿"根因）。
4. **join 走独立端口 7782**：同机多 socket bind 同端口时单播投递归属不确定（曾致 host 收不到 join、玩家列表为空）。
5. **ReuseAddress**：同机双实例都 bind 7781/7782 时必需（真机两设备无此冲突）。
6. **DataStreamWriter 是 ref struct**：发送 API 不能包 lambda/闭包，全部写成直接循环。
7. **MotionDirt**：判定用 isPlaying（isStopped 依赖粒子死光会跳过判定）；初始化 Stop(true, StopEmittingAndClear)。壳无 Rigidbody，用帧间位移差分测速。
8. **双 Editor 失焦 InputSystem 键盘冻结**：client 会持续发"最后按住的键"（用户已确认接受）。
9. **hostTime 用 unscaledTime**：与广播节拍同基准；慢放（timeScale=0.12）/暂停期间两端插值不失真。
10. **心跳计时必须在每帧路径累计**：放 tick 段内非 tick 帧不累计，历史假象"18 秒一次 355 包/s"。
11. **TMP Clear Dynamic Data on Build=1**：每次构建清空 SIMHEI 动态字体图集（资产显示 modified 属正常，动态字体运行时按需重烘；URP 预过滤设置同理被构建更新）。
12. **UTP 引擎要点**：`ScheduleUpdate().Complete()` 每帧推进入口；新连接用 `Accept()` while 循环取出；消费用 `connection.PopEvent(driver, out stream)`（裸 `PopEvent` 无事件会写空 out 参数）。
13. **幽灵连接**：客户端进程强退无 Disconnect 事件，残留无害（正常断开已收尾）。
14. **防火墙伪装成代码 bug**：设备防火墙按 exe 路径/网络位置（专用/公用）放行——换包路径或网络判成"公用"时旧规则不生效。症状"**房间可见但加入/进入全灭**"（host 出站广播不受阻、client→host 入站被拦）曾被误判为协议 v2 问题。**联机异常先查两端防火墙（设备差异会伪装成代码回归），再查代码。**
15. **共享字段一值两用**：`Ballistic_Laser.bulletRadius` 同时是碰撞半径与预览线宽——被调成 0 后激光预览线整条"消失"（线宽=半径×2=0）。已修：线宽与半径解耦（最小 0.1），资产值可自由调。

## 四、验证状态与遗留

**已验（双 Editor 回环）**：迷宫同构、实体注册表（spawn/despawn/池化子弹/中途加入补发）、client 上行 1v1、比分/暂停广播、SessionEnd、击杀爆炸/回合特写/扬尘、瞄准线 power 同步、client 开火音、局域网发现（列表/等待→开始/中途加入）、换局、单机回归。
**已验（首次打包）**：Windows standalone（Build Profiles；场景表 Menu/Main/Client，Menu 第 0；Windowed）。
**已验（真机，2026-09-12）**：真机局域网联机（发现 → 连接全链路，用户实测）。

遗留与已知限制：
- client 中途加入拿不到当前比分（比分全量随 RoundEnd/RoundStart 下发，接入补发未带）。
- 无重连：client 断线 → SessionEnded → 回菜单；host 不会等你。
- 胜利横幅后置（RoundEnd 的 winner 已解析未外传，扩事件即可）。
- 多 client 未支持（1v1 单化身；广播侧本就可多收）。
- host 端口（7783）被设备上残留进程占用时静默开不了房——排障先 `netstat -ano | findstr 7783` 看端口归属。
- `TankAI.ExecuteShoot` 的 `//Fire()` 仍注释（用户嫌 AI 强，恢复即解注）。
- 房间密码/踢人/条目美术、client 名字展示（join 已携带）、Missile 视觉打磨——均后置。

## 五、真机联机（2026-09-12 已验证）与排障备忘

1. 真机局域网已验证可连接（发现 → 连接全链路，用户实测）。
2. 防火墙是头号排障方向：Windows 规则**按程序（exe）路径生效、按网络位置（专用/公用）筛选**——换包路径或网络判成"公用"时旧规则不生效。被拦的表现：看不到房间，或"**房间可见但加入/进入全灭**"（host 出站广播不受阻、client→host 入站被拦）。**两台设备防火墙状态不同 = 症状不对称**；首次运行弹窗勾"专用网络"。
3. 排障第一现场：`%USERPROFILE%\AppData\LocalLow\<CompanyName>\<ProductName>\Player.log`。
4. 已知无害现象：host 也会收到自己的公告（界面层面规避）；VM 里若显卡驱动不佳可能需软件渲染。

## 六、开发方式纪律（沿用）

- **忒修斯之船**：从已知可工作版本出发、一次只加一个最小差异、双端当场验证；联机大方向先听用户构想再动手，不自己另起炉灶。
- 改协议字节布局 = 三处同步（写端/读端/长度常量）+ 文件头注释布局图。
- 场景接线由用户在编辑器完成；新 .cs 需先进 Assembly-CSharp.csproj 再 dotnet build（Unity 会重写 csproj）。
- ParrelSync 双 Editor 为日常测试环境（主项目=host、clone=client，回环 127.0.0.1）。

## 七、其它功能线状态（未变）

计分 HUD / 暂停面板（UI Toolkit，PauseController 收口）/ 音频（AudioManager prefab 单例、PlayerPrefs 双音量）/ 粒子（dirt、爆炸）/ 结算慢放 + 相机特写 / 全量 Config 化 / AI 拾取道具——均已完成并 commit。音频与迷宫分层纪律详见 CLAUDE.md。
